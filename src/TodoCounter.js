import React from 'react'
import './TodoCounter.css';

/**Es el codigo del la etiqueta del mensaje de tareas completadas  */
function TodoCounter() {
    return (
        <h2 className="TodoCounter">Tareas  </h2>
    );
}

export { TodoCounter };